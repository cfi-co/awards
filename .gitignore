# Build artifacts / never-commit secrets (defence in depth — secrets live
# outside the repo in /root, but make accidental commits impossible).
scripts/.commit-plan
*.pem
*.key
*_rsa
*_ed25519
.env
scripts/.wayback-cache.tsv
